/**
 * @deprecated Import from `@/features/checkout/useMercadoPago` instead.
 * Kept for backward compatibility during FSD migration.
 */
export { useMercadoPago } from '@/features/checkout/useMercadoPago'
